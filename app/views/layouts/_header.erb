<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Plantera</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Plant Species", plant_species_index_url, class: "nav-link #{"active" if active == :plant_species}" %>
        </li>

        <li class="nav-item">
          <%= link_to "My Collection", collected_plants_url, class: "nav-link #{"active" if active == :collected_plants}" %>
        </li>

        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            My Account
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><h5 class="dropdown-header"><%= current_user.email %></h5></li>
            <li><hr class="dropdown-divider"></li>
            <li>
            <%= form_with(url: destroy_user_session_path , method: :delete) do |f| %>
              <%= f.submit "Logout", class: 'dropdown-item btn btn-outline-danger' %>
            <% end %>
            </li>
          </ul>
        </li>
      </ul>
      <%= form_with url: search_plant_species_index_url, method: :get, class: "d-flex" do |form| %>
        <%= form.text_field :query, value: @query, type: "search", placeholder: "Search Plants", class: "form-control me-2", 'aria-label' => "Search" %>
        <%= form.submit "Search", class: "btn btn-outline-success" %>
      <% end %>
    </div>
  </div>
</nav>
